# ANTI-AI DESIGN SYSTEM — ארכיטקטורה מלאה

## תובנת היסוד

"לא נראה AI" ו"נראה כמו עצמו" הם שני מרחבים שונים:

- מרחב שלילי (לא AI): מנוע העלמת tells. מגיע ל-לא מזוהה כ-AI.
- מרחב חיובי (עצמו): מנוע זהות מהתחום. מגיע ל-מזוהה כמותג יחיד.

רוב הניסיונות בעולם עובדים רק במרחב השלילי (הימנעות). זה מייצר מוצר
נקי אבל חסר פנים. המוצר שאנחנו בונים חייב את שני המנועים, והוכחה שהם עובדים יחד.

**למה בכלל יש tells?** כל tell הוא טביעת אצבע של מיצוע:
מודלים ממטבים ממוצע טוב סביר, והממוצע מופיע בפלט.
כל tell בקטלוג הוא מקום שבו הממוצע בולט החוצה.

## 5 השכבות של המערכת

1. **KNOWLEDGE** — tell_registry.py: 39 tells, 9 שכבות.
   לכל tell יש signal (זיהוי) + counter (היפוך) + weight.
2. **DETECTION** — detector.py (תמונות, 95%), text_detector + corpus_detector
   (טקסט, מוכח). חסרים: code_detector, design_token_detector.
3. **IDENTITY** — identity_extractor.py: DNA מהתחום עצמו:
   motif / material / signature / voice / palette_logic / hierarchy / rhythm.
4. **GENERATION** — designer/redesigner/codegen/pagegen (עיצוב),
   text_rewriter (טקסט, מצב עדין + מצב עריכה).
   חסר: composer — יצירה מאפס תחת אילוצי counter + DNA.
5. **VALIDATION** — לולאת detect, counter, re-detect.
   קריטריון כפול: clean (אין tells) + identity-strong.

## מה מוכח עד כה (לא טענות — תוצאות)

| יכולת | הוכחה |
|---|---|
| זיהוי תמונות AI | 95% על כיול אמיתי (20 תמונות, FINDINGS) |
| זיהוי טקסט AI | precision 1.00 — אפס אזעקות שווא על תנך/משנה/ביאליק |
| זיהוי אוספים | remez 0.81, pshat 0.68 — כולל מה שבלתי-נראה ברמת משפט |
| שכבה נגדית לטקסט | 0.81 ל-0.49, retention 0.935, דטרמיניסטי |
| הגבול הכנה | אוטומציה מגיעה עד uncertain; מעבר דורש תוכן/סכמה |

## הפייפליין לשושנה (הדגמת הקצה)

- A. AUDIT — כל הגלאים על הכל: קלפים, טקסטים, globals.css, קומפוננטות (חלקית בוצע)
- B. DNA — עץ החיים כעקרון פריסה, קלף+דיו, מחוות הוורד, קול לומד שקט (בוצע)
- C. STRUCTURE — פריסת emanation, היררכיה של האצלה, grain, צללים חמים (בתכנון)
- D. CONTENT — editorial_diversify + מגוון סכמה + סגנונות סגירה (מוכח חלקית)
- E. VALIDATION — corpus_detector + tell scan + ציון משוקלל (בתכנון)

## מה הופך את התוצר למקורי לחלוטין עם סגנון משלו

1. גזירה מהתחום, לא בחירה מספרייה: כל החלטה מצביעה חזרה אל מבנה התחום
   (22 נתיבים, 10 ספירות, 5 עלי כותרת).
2. מחוות חתימה אחת: פריחת עלי הכותרת של הקריאה היומית. רק למוצר הזה.
3. חומר מחייב: קלף ודיו — כל משטח מציית לחומר אחד.
4. קול כתוב מחייב: לומד שקט, מקורות נקראים בשם, בלי קריאות התפעלות.
5. מדידות: המערכת עצמה מדרגת את התוצר. ניקוי גבוה הוא מספר, לא טעם.

## כללי הברזל של המערכת

1. כל טענה דורשת בדיקה משוחזרת (לקח סבב 2.5).
2. כל שכבה נגדית חייבת מדד retention / שימור-משמעות.
3. בלי יצירת תוכן בשכבות האוטומטיות — רק פעולות מבניות/עריכה.
4. מה שנשאר אחרי האוטומציה = העבודה האנושית, מוגדרת במדויק.
5. הזהות נמדדת גם היא — validate_dna דוחה מילים גנריות.